---
"nanoclaw-agenttrace": minor
---

Forward Anthropic summarized thinking under default `trace` (behavior change for existing enabled installs), and add opt-in `trace_full` firehose (tool inputs/results + subagent text). Raw CoT is never available from the API and never forwarded.
